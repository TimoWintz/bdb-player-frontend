<table class="table">
        <tbody>
    {{#each model as |item|}}
            <tr class="nowrap">
                <td class="track">{{item.track}} </td>
                <td>{{item.artist}}</td>
                <td>
                    {{#link-to "album" item.album_id (query-params play=item.id)}}
                        {{item.title}}
                    {{/link-to}}
                </td>
                <td> {{item.album}} </td>
                <td class="duration"> {{format-duration item.length "sec"}} </td>
            </tr>
    {{/each}}
        </tbody>
    {{#infinity-loader infinityModel=model}}
    {{/infinity-loader}}
</table>
