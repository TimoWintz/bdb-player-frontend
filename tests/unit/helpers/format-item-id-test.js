
import { formatItemId } from 'music-player/helpers/format-item-id';
import { module, test } from 'qunit';

module('Unit | Helper | format item id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatItemId([42]);
  assert.ok(result);
});

