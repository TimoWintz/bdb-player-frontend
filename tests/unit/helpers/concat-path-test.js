
import { concatPath } from 'music-player/helpers/concat-path';
import { module, test } from 'qunit';

module('Unit | Helper | concat path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = concatPath([42]);
  assert.ok(result);
});

